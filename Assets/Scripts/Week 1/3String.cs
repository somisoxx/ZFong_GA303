
using UnityEngine;

public class String : MonoBehaviour
{
    public string three;
    public string two;
    public string one;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log(three + two + one);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
